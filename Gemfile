source 'https://rubygems.org'
ruby '2.2.0'

gem 'oauth', '~> 0.4.7'

# Include redis gem if you want to use redis as a storage container
# gem 'redis', '~> 3.2.1', require: false

group :test do
  gem 'rspec', '~> 3.2.0'
end
